# 工作1: [已完成]
1.将uNetwork.html, 使用mswjs, 实现本地npm run dev运行时, 能够加载模拟数据.
2.mswjs以及模拟数据只在本地npm run dev运行时生效, 不会影响生产环境.

# 工作2: [已完成] 修复当前工作1中实现的的msw的使用方式, 不需要通过fetch来捕获, 而是直接注入到table中初始化展示.

# 工作3: [已完成] 我想在uNetwork.html页面下载接口数据时, 能够过滤一些url的数据, 让这些数据下载到json数据中.
1. 先设计一下具体的过滤数据的设置方式, 待我确认之后再进行开发.

# 工作4: [已完成] 修复和优化
1.[已完成]在导出时, 我选择排除的过滤规则, 但是实际上导出的数据里面变成只包含排除的url数据

# 工作5: [已完成] 增加publish脚本
在ajax-tools/scripts文件夹中增加publish.ts脚本, 用于将当前源码目录的用于发布的文件, 单独提取到一个`ajax-tools/publish` 文件夹中.

需要提取的文件参考`/Users/ryan.wang/Work/google-extensions/ajax-tools-dist` 文件夹中的所有文件.